services:
  db:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: patchworks
      POSTGRES_PASSWORD: patchworks
      POSTGRES_DB: patchworks
    ports:
      - "127.0.0.1:55432:5432"
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data:
